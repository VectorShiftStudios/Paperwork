using System;
using System.Collections.Generic;
using UnityEngine;

public class CDebugLevels
{
	public bool mDebug;
	public int mDebugStatLevel;
	public bool mDebugNoFow;
	public bool mDebugPrims;
	public bool mDebugSelectedUnits;
	//public bool mDebugWorldGrid;

	/*
	public void UpdateInput()
	{
		//if (Input.GetKeyUp(KeyCode.F12)) mDebug = !mDebug;
		if (Input.GetKeyUp(KeyCode.F1)) mDebugStatLevel = ++mDebugStatLevel % 3;
		if (Input.GetKeyUp(KeyCode.F2)) mDebugSelectedUnits = !mDebugSelectedUnits;
		if (Input.GetKeyUp(KeyCode.F5)) mDebugPrims = !mDebugPrims;
		if (Input.GetKeyUp(KeyCode.F6)) mDebugNoFow = !mDebugNoFow;
		if (Input.GetKeyUp(KeyCode.F7)) mDebugWorldGrid = !mDebugWorldGrid;
	}
	*/
}