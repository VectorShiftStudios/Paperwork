using UnityEngine;
using System.Collections;

public class CParticles : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GameObject.Destroy(gameObject, 1.0f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
